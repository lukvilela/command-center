{
  "name": "tryevo-board-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard executivo do board Trello TryEvo",
  "scripts": {
    "refresh": "node refresh.js",
    "serve": "npx serve -l 8000 .",
    "e2e": "playwright test",
    "dev": "netlify dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "netlify-cli": "^26.1.0"
  }
}
